# Example environment configuration for server
# Replace <db_password> with your actual MongoDB Atlas password
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Secret used to sign JWTs
JWT_SECRET=changeme
# S3/R2 configuration
S3_BUCKET=<bucket-name>
S3_REGION=<region>
S3_ACCESS_KEY_ID=<access-key-id>
S3_SECRET_ACCESS_KEY=<secret-access-key>
S3_ENDPOINT=<custom-endpoint-optional>
S3_PUBLIC_URL=<public-base-url>

# Email configuration
# Option 1: SendGrid
SENDGRID_API_KEY=<sendgrid-api-key>
NOTIFY_FROM_EMAIL=noreply@example.com

# Option 2: SMTP (used by nodemailer if SENDGRID_API_KEY is not set)
SMTP_HOST=<smtp-host>
SMTP_PORT=587
SMTP_USER=<smtp-username>
SMTP_PASS=<smtp-password>
EMAIL_FROM=${NOTIFY_FROM_EMAIL}
